# Backend Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Frontend (Next.js)                       │
│              (React Components + TypeScript)                 │
└────────────────────┬────────────────────────────────────────┘
                     │
        HTTP Request │ JSON
                     ▼
┌─────────────────────────────────────────────────────────────┐
│           Flask REST API (app.py)                           │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐  ┌──────────────────┐                 │
│  │   Endpoints     │  │  MLModelTrainer  │                 │
│  ├─────────────────┤  ├──────────────────┤                 │
│  │ • /api/train    │  │ • Algorithm Mgmt │                 │
│  │ • /api/models   │  │ • Data Prep      │                 │
│  │ • /api/predict  │  │ • Evaluation     │                 │
│  │ • /api/batch    │  │ • Selection      │                 │
│  │ • /api/parse    │  │ • Justification  │                 │
│  │ • /api/health   │  │                  │                 │
│  └─────────────────┘  └──────────────────┘                 │
└────────┬──────────────────────────────────────────┬─────────┘
         │                                          │
    MySQL │                                 Model │ Files
    Queries│                                Storage│
         │                                          │
         ▼                                          ▼
┌─────────────────────────┐          ┌──────────────────────┐
│   MySQL Database        │          │  File System         │
├─────────────────────────┤          ├──────────────────────┤
│ • models table          │          │ models/              │
│ • training_results      │          │ ├─ model_1.pkl      │
│ • predictions table     │          │ └─ metadata_1.json  │
└─────────────────────────┘          └──────────────────────┘
```

## 📊 Data Flow Diagram

### Training Flow
```
User Input (CSV)
      │
      ▼
┌──────────────────┐
│  CSV Parser      │
│ (robust_read)    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Data Validation  │
└────────┬─────────┘
         │
         ▼
┌──────────────────────────────────────┐
│ MLModelTrainer                       │
├──────────────────────────────────────┤
│ 1. Preprocess Data                   │
│    - Handle missing values           │
│    - Encode categorical features     │
│ 2. Train All Algorithms              │
│    - 7-8 different models            │
│ 3. Evaluate Each Model               │
│    - Classification metrics          │
│    - Regression metrics              │
│ 4. Select Best                       │
│    - Compare scores                  │
│ 5. Generate Justification            │
│    - Explain selection               │
└────────┬─────────────────────────────┘
         │
         ▼
┌──────────────────────┐
│ ModelSerializer      │
├──────────────────────┤
│ • Save model as PKL  │
│ • Save metadata JSON │
│ • Create ID link     │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ DatabaseManager      │
├──────────────────────┤
│ • Store model info   │
│ • Store metrics      │
│ • Store algorithm    │
│ • Store file paths   │
└────────┬─────────────┘
         │
         ▼
    Return Model ID to Frontend
```

### Prediction Flow
```
User Input (Features)
      │
      ▼
┌──────────────────────┐
│ Load from DB         │
│ - Get model path     │
│ - Get metadata       │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────┐
│ ModelSerializer          │
├──────────────────────────┤
│ • Load model from file   │
│ • Load preprocessing     │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────────────┐
│ PreprocessingPipeline        │
├──────────────────────────────┤
│ • Encode categorical         │
│ • Scale features             │
│ • Validate input             │
└────────┬─────────────────────┘
         │
         ▼
┌──────────────────────────┐
│ Trained Model            │
│ (Sklearn Estimator)      │
├──────────────────────────┤
│ • predict()              │
└────────┬─────────────────┘
         │
         ▼
┌──────────────────────┐
│ Postprocess Output   │
│ - Decode if needed   │
│ - Format result      │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│ Log Prediction       │
│ - Store in DB        │
│ - Audit trail        │
└────────┬─────────────┘
         │
         ▼
    Return Prediction to Frontend
```

## 🗄️ Database Schema Diagram

```
┌──────────────────────────────────────────┐
│           models table                   │
├──────────────────────────────────────────┤
│ PK  id                     INT            │
│     model_name             VARCHAR        │
│     description            TEXT           │
│     model_type             ENUM           │
│     best_algorithm         VARCHAR        │
│     metrics               JSON            │
│     justification         TEXT            │
│     model_file_path       VARCHAR         │
│     input_features        JSON            │
│     output_feature        VARCHAR         │
│     accuracy              FLOAT           │
│     created_at            TIMESTAMP       │
│     updated_at            TIMESTAMP       │
│  ┌─────────────┐                          │
│  │ Indexes:    │                          │
│  │ idx_name    │                          │
│  │ idx_type    │                          │
│  │ idx_created │                          │
│  └─────────────┘                          │
└──────────────────┬───────────────────────┘
                   │ 1:N
                   │
        ┌──────────┴──────────┐
        ▼                     ▼
┌─────────────────────┐  ┌──────────────────┐
│  training_results   │  │  predictions     │
├─────────────────────┤  ├──────────────────┤
│ PK  id              │  │ PK  id           │
│ FK  model_id        │  │ FK  model_id     │
│     algorithm_name  │  │     input_data   │
│     metrics (JSON)  │  │     prediction   │
│     score           │  │     created_at   │
└─────────────────────┘  └──────────────────┘
```

## 🔌 API Endpoint Map

```
┌─────────────────────────────────────────────────────────────┐
│                    Flask API Routes                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Training & Storage                                        │
│  ┌────────────────────────────────────────────────────┐   │
│  │ POST /api/train                                    │   │
│  │   Input: CSV + features + target                   │   │
│  │   Output: Model ID + results + metrics             │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  Model Management                                          │
│  ┌────────────────────────────────────────────────────┐   │
│  │ GET  /api/models              (List all)           │   │
│  │ GET  /api/models/<id>         (Get details)        │   │
│  │ PUT  /api/models/<id>         (Update metadata)    │   │
│  │ DELETE /api/models/<id>       (Delete model)       │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  Predictions                                               │
│  ┌────────────────────────────────────────────────────┐   │
│  │ POST /api/models/<id>/predict        (Single)      │   │
│  │ POST /api/models/<id>/batch-predict  (Multiple)    │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
│  Utilities                                                 │
│  ┌────────────────────────────────────────────────────┐   │
│  │ POST /api/parse-csv       (Analyze CSV)            │   │
│  │ GET  /api/health          (Health check)           │   │
│  └────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Class Hierarchy

```
Flask Application (app.py)
├── MLModelTrainer
│   ├── get_algorithms()         → Dict[str, Model]
│   ├── preprocess_data()        → (X, y)
│   ├── evaluate_classification()→ Dict[str, float]
│   ├── evaluate_regression()    → Dict[str, float]
│   ├── train_and_evaluate()     → Dict[results]
│   └── generate_justification() → str
│
├── ModelSerializer
│   ├── save_model()             → str (path)
│   ├── load_model()             → Model
│   ├── save_metadata()          → str (path)
│   ├── load_metadata()          → Dict
│   └── serialize_preprocessing()→ Dict
│
├── PreprocessingPipeline
│   ├── preprocess_input()       → np.array
│   ├── postprocess_output()     → result
│   └── serialize_preprocessing()→ Dict
│
└── DatabaseManager
    ├── connect()                → None
    ├── create_tables()          → None
    ├── save_model()             → int (id)
    ├── get_model()              → Dict
    ├── get_all_models()         → List[Dict]
    ├── update_model()           → bool
    ├── delete_model()           → bool
    ├── save_prediction()        → None
    └── get_training_results()   → List[Dict]
```

## 🔄 Request/Response Flow

### Training Request
```
Frontend
  │
  ├─ model_name: str
  ├─ model_type: 'classification' | 'regression'
  ├─ csv_data: str
  ├─ input_features: List[str]
  ├─ output_feature: str
  └─ description: str
       │
       ▼
   app.py:train_model()
       │
       ├─ Parse CSV
       ├─ Create MLModelTrainer
       ├─ Train & Evaluate
       ├─ Serialize Model
       ├─ Save to DB
       └─ Generate Response
       │
       ▼
   {
     "success": true,
     "model_id": int,
     "results": [...],
     "best_model": str,
     "justification": str
   }
```

### Prediction Request
```
Frontend
  │
  ├─ model_id: int
  └─ data: Dict[str, any]
       │
       ▼
   app.py:predict()
       │
       ├─ Get model from DB
       ├─ Load model from disk
       ├─ Preprocess input
       ├─ Make prediction
       ├─ Log to DB
       └─ Generate Response
       │
       ▼
   {
     "success": true,
     "model_id": int,
     "prediction": result
   }
```

## 📁 File Organization

```
back-end/
│
├─── Core Application
│    ├── app.py                  (Flask routes + trainer)
│    ├── database.py             (DB management)
│    ├── model_serializer.py     (Model persistence)
│    └── config.py               (Configuration)
│
├─── Configuration
│    ├── requirements.txt         (Dependencies)
│    ├── .env.example            (Environment template)
│    └── .gitignore              (Git rules)
│
├─── Documentation
│    ├── README.md               (Overview)
│    ├── INDEX.md                (Navigation guide)
│    ├── SETUP_GUIDE.md          (Installation)
│    ├── API_DOCUMENTATION.md    (API reference)
│    ├── IMPLEMENTATION_SUMMARY.md(Technical)
│    ├── DEPLOYMENT_GUIDE.md     (Deployment)
│    └── COMPLETION_SUMMARY.md   (Status)
│
├─── Examples
│    └── example_client.py       (Usage examples)
│
├─── Reference Algorithms
│    ├── algorithms/
│    │   ├── classification/
│    │   │   ├── logistic_regression.py
│    │   │   ├── decision_tree_classifier.py
│    │   │   ├── random_forest_classifier.py
│    │   │   ├── gradient_boosting_classifier.py
│    │   │   ├── svm_classifier.py
│    │   │   ├── naive_bayes_classifier.py
│    │   │   └── knn_classifier.py
│    │   └── regression/
│    │       ├── linear_regression.py
│    │       ├── ridge_regression.py
│    │       ├── lasso_regression.py
│    │       ├── decision_tree_regressor.py
│    │       ├── random_forest_regressor.py
│    │       ├── gradient_boosting_regressor.py
│    │       ├── svr_regressor.py
│    │       └── knn_regressor.py
│
└─── Generated/Runtime
     └── models/                 (Trained models + metadata)
         ├── model_1_*.pkl
         └── metadata_1.json
```

## 🚀 Deployment Architecture

```
                    User/Client
                         │
                    ┌────▼─────┐
                    │  Browser  │
                    └────┬─────┘
                         │ HTTPS
                    ┌────▼────────────────┐
                    │   Frontend (CDN)    │
                    │   (Next.js Build)   │
                    └────┬─────────────────┘
                         │ API Requests
                    ┌────▼────────────────────┐
                    │  Reverse Proxy          │
                    │  (Nginx)                │
                    └────┬────────────────────┘
                         │ Load Balance
        ┌────────────────┼────────────────┐
        │                │                │
    ┌───▼───┐        ┌──▼───┐        ┌──▼───┐
    │Worker1│        │Worker│        │Worker│
    │       │        │  2   │        │  3   │
    │Gunicorn│       │Gunicorn│     │Gunicorn│
    └───┬───┘        └──┬───┘        └──┬───┘
        │               │               │
        └───────────┬───┴───────────────┘
                    │ Database Queries
              ┌─────▼────────┐
              │ MySQL Server │
              │  (Primary)   │
              └──────────────┘
              
Plus: Redis (optional caching)
      Monitoring & Logging
      Backups
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│  Request Validation Layer               │
│  ├─ Input validation                    │
│  ├─ Type checking                       │
│  └─ Size limits                         │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Authentication Layer (Future)          │
│  ├─ JWT tokens                          │
│  ├─ Session management                  │
│  └─ Rate limiting                       │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Application Layer                      │
│  ├─ Error handling                      │
│  ├─ Business logic                      │
│  └─ Data processing                     │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Database Layer                         │
│  ├─ Connection pooling                  │
│  ├─ Parameterized queries               │
│  └─ Transaction management              │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│  Data Storage                           │
│  ├─ MySQL database                      │
│  └─ File system                         │
└─────────────────────────────────────────┘
```

## 📊 Metrics & Monitoring

```
┌────────────────────────────────────────┐
│  Application Metrics                   │
├────────────────────────────────────────┤
│ ┌─ Request Metrics                     │
│ │  ├─ Response time                    │
│ │  ├─ Error rate                       │
│ │  └─ Throughput                       │
│ │                                      │
│ ├─ Model Metrics                       │
│ │  ├─ Training time                    │
│ │  ├─ Algorithm performance            │
│ │  └─ Prediction accuracy              │
│ │                                      │
│ ├─ System Metrics                      │
│ │  ├─ CPU usage                        │
│ │  ├─ Memory usage                     │
│ │  ├─ Disk space                       │
│ │  └─ Network I/O                      │
│ │                                      │
│ └─ Database Metrics                    │
│    ├─ Query time                       │
│    ├─ Connection pool                  │
│    └─ Storage usage                    │
│                                        │
└────────────────────────────────────────┘
```

---

**Version**: 1.0.0  
**Last Updated**: November 27, 2025  
**Architecture Status**: Complete ✅
